# All Pass 行李箱販售系統 - 功能詳細文檔

**項目名稱：** All Pass 行李箱專賣  
**系統日期：** 2026 年  
**主要功能：** 前台商城 + 後台管理系統

---

## 📋 目錄
1. [系統整體架構](#系統整體架構)
2. [前台功能模組](#前台功能模組)
3. [後台功能模組](#後台功能模組)
4. [數據庫查詢工具](#數據庫查詢工具)
5. [安全機制說明](#安全機制說明)
6. [用戶流程圖](#用戶流程圖)

---

## 🏗️ 系統整體架構

### 核心構成
```
┌─────────────────────────────────────────────────────┐
│          All Pass 行李箱販售系統                      │
├──────────────────┬──────────────────┬───────────────┤
│   前台購物系統    │  會員認證系統     │  後台管理系統  │
├──────────────────┼──────────────────┼───────────────┤
│ • 商品展示       │ • 用戶登入       │ • 商品新增    │
│ • 商品分類       │ • 用戶註冊       │ • 庫存管理    │
│ • 購物車         │ • 密碼加密       │ • 分類管理    │
│ • 支付流程       │ • Session管理    │ • 圖片上傳    │
└──────────────────┴──────────────────┴───────────────┘
```

### 數據庫連線
- **主機：** localhost
- **用戶名：** root
- **密碼：** (無密碼)
- **數據庫：** all_pass_db

---

## 🛒 前台功能模組

### 1️⃣ **index.php** - 前台首頁 & 商城首頁

#### 功能概述
- 展示行李箱商品的主要購物界面
- 提供用戶登入/登出入口
- 顯示精選商品推薦
- 購物車系統集成

#### 主要特色
| 功能 | 說明 |
|------|------|
| **Hero Section** | 大型背景圖輪播，展示品牌形象 |
| **Header導航** | 品牌logo、搜尋框、購物車、用戶選單 |
| **Product Grid** | 3欄響應式商品網格 (平板2欄、手機1欄) |
| **信任徽章** | 展示「破箱保修」「免運」「分期付款」等福利 |
| **搜尋功能** | 實時搜尋商品功能 |

#### 技術實現
```php
// Session 檢查（用於判斷用戶是否已登入）
session_start();

// 數據庫連線
$conn = new mysqli("localhost", "root", "", "all_pass_db");
```

#### 頁面構成
- **頂部導航列** - 品牌名稱、搜尋、購物車、用戶菜單
- **英雄區域 (Hero)** - 全屏背景圖，強調品牌
- **信任徽章區** - 三個主要銷售亮點
- **商品展示區** - 動態加載的商品卡片

#### 樣式亮點
```css
/* 商品懸停放大效果 */
.product-img { 
    transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.product-card:hover .product-img { 
    transform: scale(1.08); 
}
```

---

### 2️⃣ **login.php** - 用戶登入頁面

#### 功能概述
- 一般消費者（非管理員）登入
- 直接查詢 `users` 表格
- 密碼使用 `password_verify()` 進行驗證

#### 登入流程
```
用戶輸入 Email + 密碼
         ↓
搜尋 users 表格
         ↓
檢查密碼是否正確
         ↓
【成功】存入 Session → 跳轉首頁
【失敗】顯示錯誤訊息
```

#### 關鍵代碼
```php
$sql = "SELECT * FROM users WHERE email = '$email'";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    $row = $result->fetch_assoc();
    
    // 🌟 關鍵：使用 password_verify() 比對密碼
    if (password_verify($password, $row['password_hash'])) {
        // 存入 Session
        $_SESSION['user_id'] = $row['user_id'];
        $_SESSION['user_name'] = $row['name'];
        
        // 導向首頁
        header("Location: index.php");
        exit();
    } else {
        $error_message = "密碼錯誤，請再試一次！";
    }
}
```

#### 錯誤處理
| 錯誤類型 | 顯示訊息 |
|---------|--------|
| 帳號不存在 | "找不到此帳號，請先前往註冊！" |
| 密碼錯誤 | "密碼錯誤，請再試一次！" |

#### 頁面元素
- **Logo 區域** - 品牌標識 + 副標題「會員登入」
- **Email 欄位** - 用戶郵箱/帳號輸入
- **密碼欄位** - 加密輸入框
- **記住我** - 勾選框（可選功能）
- **忘記密碼** - 連結（可擴展功能）
- **登入按鈕** - 藍綠色按鈕，懸停變紅
- **註冊連結** - 導向 register.php

---

### 3️⃣ **register.php** - 用戶註冊頁面

#### 功能概述
- 新用戶建立帳號
- 數據驗證與安全檢查
- 密碼 Hash 加密存儲

#### 註冊流程
```
用戶填寫表單
  ├─ 姓名
  ├─ Email
  ├─ 電話 (選填)
  ├─ 密碼
  └─ 確認密碼
        ↓
【驗證】密碼是否一致
        ↓
【加密】使用 password_hash()
        ↓
【檢查】Email 是否已被使用
        ↓
【成功】插入 users 表格 → 顯示成功訊息
【失敗】顯示對應錯誤
```

#### 驗證規則
```php
// 1. 檢查密碼一致性
if ($password !== $confirm_password) {
    $error_message = "兩次輸入的密碼不一致，請重新確認！";
}

// 2. 密碼 Hash 加密
$hashed_password = password_hash($password, PASSWORD_DEFAULT);

// 3. SQL 插入
$sql = "INSERT INTO users (name, email, password_hash, phone) 
        VALUES ('$name', '$email', '$hashed_password', '$phone')";

// 4. 錯誤處理
if ($conn->errno == 1062) {  // 違反 UNIQUE 約束
    $error_message = "這個電子信箱已經被註冊過囉！";
}
```

#### 錯誤代碼說明
| MySQL 錯誤碼 | 含義 | 處理方式 |
|------------|------|--------|
| 1062 | UNIQUE 約束違反 (重複郵箱) | 提示使用其他郵箱或直接登入 |
| 其他 | 系統錯誤 | 顯示詳細錯誤訊息 |

#### 表單欄位
| 欄位 | 類型 | 必填 | 備註 |
|------|------|------|------|
| 姓名 | text | ✅ | 用戶全名 |
| 電話 | tel | ❌ | 可選聯絡方式 |
| Email | email | ✅ | 登入帳號，唯一值 |
| 密碼 | password | ✅ | 需 8+ 字元 |
| 確認密碼 | password | ✅ | 必須與密碼相同 |

#### 成功訊息
```
🎉 註冊成功！歡迎加入 All Pass，請前往登入。
```

---

### 4️⃣ **logout.php** - 用戶登出功能

#### 功能概述
- 清除 Session 並完全登出
- 銷毀 Session 數據
- 跳轉回首頁

#### 實現代碼
```php
session_start();

// 清空所有 Session 變數
$_SESSION = array();

// 徹底銷毀 Session
session_destroy();

// 導向首頁
header("Location: index.php");
exit();
```

#### 流程
1. 啟動 Session
2. 清空 `$_SESSION` 陣列 (移除所有用戶信息)
3. 執行 `session_destroy()` (銷毀 Session 文件)
4. 重定向至首頁

#### 清除數據
```php
// 被清除的 Session 變數
$_SESSION['user_id']   // 用戶ID
$_SESSION['user_name'] // 用戶姓名
$_SESSION['email']     // 用戶郵箱
```

---

## 👨‍💼 後台功能模組

### 5️⃣ **admin_login.php** - 管理員登入頁面

#### 功能概述
- **專為管理員設計** 的登入系統
- 查詢 `admin_users` 表格
- 檢查帳號狀態 (ACTIVE/停用)
- 密碼驗證與 Session 管理

#### 登入流程
```
管理員輸入帳號 + 密碼
       ↓
查詢 admin_users 表格
       ↓
【檢查】帳號狀態是否為 ACTIVE
       ├─ 非 ACTIVE → 顯示「帳號已停用」
       └─ ACTIVE → 繼續
       ↓
【驗證】密碼是否正確
       ├─ 正確 → 發放 Session
       ├─ 錯誤 → 顯示「密碼錯誤」
       └─ 帳號不存在 → 顯示「找不到帳號」
       ↓
【成功】跳轉至 backend.php
```

#### 關鍵代碼
```php
// 1. 查詢管理員帳號
$stmt = $conn->prepare(
    "SELECT admin_id, role_id, password_hash, status 
     FROM admin_users WHERE username = ?"
);
$stmt->bind_param("s", $username);
$stmt->execute();
$result = $stmt->get_result();

if ($user = $result->fetch_assoc()) {
    // 2. 檢查帳號狀態
    if ($user['status'] !== 'ACTIVE') {
        $error = "❌ 該帳號已被停用，請聯絡系統負責人。";
    } 
    // 3. 驗證密碼
    else if (password_verify($password, $user['password_hash'])) {
        $_SESSION['admin_id'] = $user['admin_id'];
        $_SESSION['admin_username'] = $username;
        $_SESSION['role_id'] = $user['role_id'];
        
        header("Location: backend.php");
        exit();
    } else {
        $error = "❌ 密碼錯誤！";
    }
}
```

#### Session 變數
| Session 變數 | 內容 | 用途 |
|-------------|------|------|
| `$_SESSION['admin_id']` | 管理員ID | 身份識別 |
| `$_SESSION['admin_username']` | 管理員帳號 | 顯示在後台 |
| `$_SESSION['role_id']` | 角色ID | 權限判斷 |

#### 錯誤訊息
```php
"❌ 該帳號已被停用，請聯絡系統負責人。"
"❌ 密碼錯誤！"
"❌ 找不到該管理員帳號！"
```

#### 頁面設計
- **登入框顏色** - 白色背景，深灰色 (#2c3e50) 為主色
- **登入按鈕** - 暗紅色 (#db6b6b)，懸停變深紅
- **響應式設計** - 最大寬度 350px

---

### 6️⃣ **backend.php** - 後台主框架與功能選單

#### 功能概述
- **後台入口總頁**，登入後會先進入這個頁面
- 左側顯示完整功能選單，右側顯示當前模組內容
- 以 `?page=` 切換不同後台模組
- 目前商品管理是第一個已完成的功能模組

#### 功能選單結構
1. 📊 營運儀表板 (Dashboard)
2. 📦 商品管理 (Product Management)
3. 🏷️ 分類管理 (Category Management)
4. 🧾 訂單管理 (Order Management)
5. 👥 會員與客服管理 (Member Management)
6. 📢 行銷與內容管理 (Marketing & Content)
7. ⚙️ 系統與權限管理 (System & Security)
8. 🔐 管理者資料

#### 頁面切換方式
```php
$page = isset($_GET['page']) ? $_GET['page'] : 'products';
```

#### 安全設計
- 只有登入管理員才能進入
- 使用白名單限制可載入的頁面
- 避免任意路徑載入造成風險

#### 主要頁面角色
| 頁面 | 作用 |
|------|------|
| `backend.php?page=products` | 商品管理主畫面 |
| `backend.php?page=profile` | 管理者資料與登出 |
| `backend.php?page=dashboard` | 儀表板預留頁 |
| `backend.php?page=categories` | 分類管理預留頁 |
| `backend.php?page=orders` | 訂單管理預留頁 |
| `backend.php?page=members` | 會員與客服管理預留頁 |
| `backend.php?page=marketing` | 行銷與內容管理預留頁 |
| `backend.php?page=system` | 系統與權限管理預留頁 |

---

### 6️⃣-A **profile.php** - 管理者資料區

#### 功能概述
- 顯示目前登入的管理員帳號
- 提供快速登出按鈕
- 作為後台個人區入口

#### 顯示內容
| 欄位 | 說明 |
|------|------|
| 帳號 | 顯示 `$_SESSION['admin_username']` |
| 角色 | 目前先固定顯示「管理員」 |
| 登出 | 導向 `admin_logout.php` |

#### 實際用途
- 管理員可以快速確認目前登入的是哪個帳號
- 也可以從這裡直接安全登出

---

### 6️⃣-B **admin_logout.php** - 管理員登出功能

#### 功能概述
- 清空管理員 Session
- 銷毀登入狀態
- 導回管理員登入頁

#### 實現代碼
```php
session_start();
$_SESSION = array();
session_destroy();
header("Location: admin_login.php");
exit();
```

#### 與前台登出的差異
- 前台 `logout.php` 會回到 `homepage/index.php`
- 後台 `admin_logout.php` 會回到 `backend/admin_login.php`

---

### 6️⃣-C **各功能模組頁面** - 後台選單內容分區

#### 已建立的頁面
| 頁面 | 狀態 | 說明 |
|------|------|------|
| `dashboard.php` | 已建立 | 營運儀表板預留頁 |
| `products.php` | 已建立 | 商品管理頁，原本的新增商品表單已移到這裡 |
| `categories.php` | 已建立 | 分類管理預留頁 |
| `orders.php` | 已建立 | 訂單管理預留頁 |
| `members.php` | 已建立 | 會員與客服管理預留頁 |
| `marketing.php` | 已建立 | 行銷與內容管理預留頁 |
| `system.php` | 已建立 | 系統與權限管理預留頁 |

#### 設計原則
- 先把整體後台架構搭好
- 再逐頁補齊每個模組的 CRUD 功能
- 避免後續功能增加時還要重改主版面

---

### 7️⃣ **products.php** - 後台商品管理界面

#### 功能概述
- **受保護的後台頁面** (需要 `admin_id` Session)
- 目前是後台中已完成的商品管理功能區塊
- 保留商品新增表單
- 從數據庫動態載入商品分類

#### 頁面構成

```
┌─────────────────────────────────┐
│      新增商品表單               │
├─────────────────────────────────┤
│ 【商品名稱】輸入框              │
│ 【分類】下拉選單 (動態載入)     │
│ 【價格】數字輸入框              │
│ 【庫存】數字輸入框              │
│ 【商品圖片】多檔上傳 (required) │
│ ☐ 首頁精選商品 (勾選框)        │
│        【發布商品】按鈕         │
└─────────────────────────────────┘
```

#### 存取控制
```php
session_start();

// 🔐 如果沒有 admin_id，直接跳轉登入頁
if (!isset($_SESSION['admin_id'])) {
    header("Location: admin_login.php");
    exit();
}
```

#### 動態分類加載
```php
// 查詢所有分類
$sql = "SELECT category_id, name FROM categories";
$result = $conn->query($sql);

// 動態生成 <option> 選項
while($row = $result->fetch_assoc()){
    echo '
    <option value="'.$row['category_id'].'">
        '.$row['name'].'
    </option>
    ';
}
```

#### 表單欄位
| 欄位 | 類型 | 必填 | 備註 |
|------|------|------|------|
| 商品名稱 | text | ✅ | - |
| 分類 | select | ❌ | 預設「不分類」 |
| 價格 | number | ✅ | 浮點數 |
| 庫存 | number | ✅ | 整數 |
| 商品圖片 | file (多檔) | ✅ | 至少上傳1張 |
| 首頁精選 | checkbox | - | 預設勾選 |

#### 表單提交
```html
<form
    action="backend_action.php"
    method="POST"
    enctype="multipart/form-data"
>
```

---

### 7️⃣ **backend_action.php** - 後台商品新增處理

#### 功能概述
- **處理商品新增的核心邏輯**
- 多步驟驗證與錯誤處理
- 圖片上傳與數據庫存儲
- **數據庫交易 (Transaction)** 確保數據一致性

#### 完整流程

```
接收表單提交
     ↓
【驗證】管理員身份 (check admin_id)
     ↓
【驗證】至少上傳1張圖片
     ↓
【啟動】數據庫交易 (BEGIN TRANSACTION)
     ↓
┌─ 步驟1：插入 products 表格
│  ├─ primary_category_id
│  ├─ name
│  ├─ slug (自動生成)
│  ├─ is_featured
│  └─ status = 'ON SHELF'
│  ↓ 取得 $product_id
│
├─ 步驟2：插入 product_variants 表格
│  ├─ product_id
│  ├─ sku_code (自動生成)
│  ├─ price
│  └─ stock_available
│
└─ 步驟3：逐個處理上傳圖片
   ├─ 驗證圖片格式 (jpg, jpeg, png, webp)
   ├─ 生成新檔名
   ├─ 搬移文件到 img/products/ 資料夾
   └─ 插入 product_images 表格 (第一張為主圖)
     ↓
【結果】commit 或 rollback
     ↓
【回應】成功/失敗訊息
```

#### 關鍵代碼解析

**1. 驗證管理員身份**
```php
if (!isset($_SESSION['admin_id'])) {
    die("Access Denied");
}
```

**2. 自動生成 Slug**
```php
$slug = strtolower(str_replace(' ', '-', $name)) . "-" . time();
// 例如: "my-luggage-1715425800"
```

**3. 自動生成 SKU 代碼**
```php
$sku_code = "AL-" . strtoupper(uniqid());
// 例如: "AL-6629A4B5C1E8F"
```

**4. 圖片驗證與上傳**
```php
// 允許格式
$allow = ['jpg', 'jpeg', 'png', 'webp'];

if (!in_array($file_ext, $allow)) {
    throw new Exception("不支援的圖片格式");
}

// 生成新檔名
$new_filename = $product_id . "_" . $key . "_" . time() . "." . $file_ext;
// 例如: "42_0_1715425800.jpg"

// 搬移文件
if (!move_uploaded_file($tmp_name, $target_path)) {
    throw new Exception("圖片上傳失敗");
}
```

**5. 第一張圖為主圖**
```php
$is_main = ($key == 0) ? 1 : 0;
// 第一個循環迭代中 $key=0，所以 is_main=1
```

**6. 數據庫交易處理**
```php
$conn->begin_transaction();

try {
    // 三個 INSERT 操作
    // 如果任何一個失敗，拋出 Exception
    
    $conn->commit();  // 全部成功
} catch (Exception $e) {
    $conn->rollback();  // 全部回滾
    echo "錯誤：" . $e->getMessage();
}
```

#### 數據庫表結構

**products 表格**
```sql
product_id (PK)
primary_category_id (FK)
name
slug
is_featured (0/1)
status (ON SHELF/OFF SHELF)
created_at
```

**product_variants 表格**
```sql
variant_id (PK)
product_id (FK)
sku_code (UNIQUE)
price
stock_available
created_at
```

**product_images 表格**
```sql
image_id (PK)
product_id (FK)
image_url
is_main (0/1)
uploaded_at
```

#### 錯誤處理
| 錯誤訊息 | 觸發條件 |
|---------|--------|
| "請至少上傳一張圖片" | `$_FILES['product_images']['name'][0]` 為空 |
| "products 新增失敗" | INSERT into products 失敗 |
| "variant 新增失敗" | INSERT into product_variants 失敗 |
| "圖片上傳錯誤" | $_FILES 的 error 值 ≠ 0 |
| "不支援的圖片格式" | 副檔名不在 ['jpg','jpeg','png','webp'] |
| "圖片資料寫入失敗" | INSERT into product_images 失敗 |

#### 成功流程
```javascript
<script>
    alert('商品新增成功');
    location.href='backend.php';  // 刷新頁面
</script>
```

---

### 8️⃣ **set_admin_account.php** - 管理員帳號初始化腳本

#### 功能概述
- **一次性運行腳本** 用於創建第一個管理員帳號
- 不需要登入直接執行
- 密碼自動 Hash 加密

#### 使用方式
1. 修改帳號密碼
2. 在瀏覽器中訪問此文件
3. 查看輸出確認成功

#### 代碼說明
```php
// 1. 設定連線
$conn = new mysqli("localhost", "root", "", "all_pass_db");

// 2. 設定帳號密碼
$user = 'admin';
$pass = 'adminOne01';

// 3. 密碼 Hash 加密
$hashed_password = password_hash($pass, PASSWORD_DEFAULT);
// 產出：$2y$10$px... (60字元)

// 4. 插入 admin_users 表格
$sql = "INSERT INTO admin_users (role_id, username, password_hash, status) 
        VALUES (1, '$user', '$hashed_password', 'ACTIVE')";
```

#### 輸出示例
```
✅ 管理員帳號建立成功！
帳號：admin
明文密碼：adminOne01
資料庫存儲的密文 (Hash)：
$2y$10$px3hX8nQ9kL2mN4oP5qR6sT7uV8wX9yZ0aB1cD2eF3gH4iJ5kL
👉 現在你可以使用 admin 去登入後台了！
```

#### 執行後的建議
- 將此文件 **刪除** 或 **移到安全位置**
- 防止他人再次訪問而創建新帳號

---

## 🔍 數據庫查詢工具

### 9️⃣ **database.php** - 自動撈資料庫檢視工具

#### 功能概述
- **自動列出數據庫中所有表格**
- **自動顯示每個表格的所有數據** (限 100 筆)
- 用於開發調試與數據檢查

#### 工作原理

```
讀取所有表格名稱 (SHOW TABLES)
         ↓
【FOR EACH 表格】
  ├─ 讀取表格的所有欄位 (SHOW COLUMNS FROM ...)
  ├─ 建立表頭 <tr><th>
  ├─ 查詢表格的所有數據 (SELECT * LIMIT 100)
  └─ 逐筆顯示數據 <tr><td>
         ↓
產出 HTML 表格
```

#### 代碼邏輯

**步驟 1：取得所有表格**
```php
$tables_result = $conn->query("SHOW TABLES");

while ($table_row = $tables_result->fetch_array()) {
    $table_name = $table_row[0];  // 例如: users, products
```

**步驟 2：取得表格欄位**
```php
$columns_result = $conn->query("SHOW COLUMNS FROM `$table_name`");
$columns = [];

echo '<tr>';
while ($col = $columns_result->fetch_assoc()) {
    $columns[] = $col['Field'];  // 存入欄位名稱
    echo '<th>' . $col['Field'] . '</th>';
}
echo '</tr>';
```

**步驟 3：取得表格數據**
```php
$data_result = $conn->query("SELECT * FROM `$table_name` LIMIT 100");

if ($data_result && $data_result->num_rows > 0) {
    while ($data_row = $data_result->fetch_assoc()) {
        echo '<tr>';
        foreach ($columns as $col_name) {
            echo '<td>' . htmlspecialchars((string)$data_row[$col_name]) . '</td>';
        }
        echo '</tr>';
    }
}
```

#### 頁面特色
| 功能 | 說明 |
|------|------|
| **動態表格** | 每個表格獨立一個 `<table>` |
| **自動編碼** | `htmlspecialchars()` 防 XSS 攻擊 |
| **限制筆數** | LIMIT 100 防止伺服器當掉 |
| **空值處理** | 若無數據顯示「此表格目前沒有任何資料」|
| **排序** | ORDER BY 1 DESC (按第一欄倒序) |

#### 安全特性
```php
// 防止 XSS 攻擊
htmlspecialchars((string)$data_row[$col_name])
// 將 < > & " ' 轉換為 HTML 實體
// 例如: <script> → &lt;script&gt;
```

#### 視覺設計
- **黑底白字表格** - 易於閱讀
- **交替行背景** - 減少視覺疲勞
- **懸停效果** - tr:hover { background-color: #f1f1f1; }
- **分欄對齊** - text-align: left

---

## 🔐 安全機制說明

### A. 密碼加密

#### 使用 `password_hash()` 函數
```php
// 加密密碼
$hashed_password = password_hash($input_password, PASSWORD_DEFAULT);
// 輸出：$2y$10$... (60字元的 bcrypt 雜湊)

// 驗證密碼
if (password_verify($input_password, $hashed_password)) {
    // 密碼正確
}
```

**優點：**
- ✅ 使用 bcrypt 算法，極難破解
- ✅ 內含 salt，同樣密碼每次加密結果不同
- ✅ 密碼不可逆，即使數據庫被洩露也無法恢復
- ✅ `password_verify()` 自動比對

#### 不安全的做法 ❌
```php
// 千萬不要這樣做！
$password_md5 = md5($password);         // MD5 易被彩虹表破解
$password_plain = $password;             // 明文存儲 = 自殺
$password_sha = sha1($password);        // SHA1 也不夠安全
```

---

### B. Session 管理

#### Session 流程
```
1. 用戶登入成功
   ↓
2. 執行 session_start()
   ↓
3. 設置 $_SESSION['user_id'] = $user_id
   ↓
4. 瀏覽器獲得 Session ID (Cookie)
   ↓
5. 每次請求，瀏覽器自動帶上 Session ID
   ↓
6. 服務器驗證 Session ID 的有效性
   ↓
7. 頁面檢查 isset($_SESSION['user_id'])
   ├─ 若有 → 允許訪問
   └─ 若無 → 跳轉登入頁
```

#### 用戶 Session 變數
```php
$_SESSION['user_id']   // 用戶ID
$_SESSION['user_name'] // 用戶姓名
```

#### 管理員 Session 變數
```php
$_SESSION['admin_id']       // 管理員ID
$_SESSION['admin_username'] // 管理員帳號
$_SESSION['role_id']        // 角色ID (權限判斷)
```

#### Session 登出清除
```php
session_destroy();  // 完全銷毀 Session
$_SESSION = array(); // 清空 Session 變數
```

---

### C. 帳號狀態檢查

#### 在 admin_login.php 中
```php
if ($user['status'] !== 'ACTIVE') {
    $error = "❌ 該帳號已被停用，請聯絡系統負責人。";
    // 即使密碼正確，也拒絕登入
}
```

**狀態值：**
| 狀態 | 含義 |
|------|------|
| ACTIVE | 正常可用 |
| INACTIVE | 已停用 |
| SUSPENDED | 被鎖定 |

---

### D. SQL 準備語句 (Prepared Statements)

#### 在 admin_login.php 中使用
```php
// 使用準備語句，防止 SQL Injection
$stmt = $conn->prepare("SELECT admin_id, role_id, password_hash, status 
                        FROM admin_users WHERE username = ?");
$stmt->bind_param("s", $username);  // "s" = string 類型
$stmt->execute();
$result = $stmt->get_result();
```

**優點：**
- ✅ 參數與 SQL 代碼分離
- ✅ 自動進行 SQL 轉義
- ✅ 完全防止 SQL Injection 攻擊

#### 不安全的做法 ❌
```php
// 千萬不要這樣寫！
$sql = "SELECT * FROM admin_users WHERE username = '$username'";
// 如果輸入: ' OR '1'='1 → SQL 語句被改變，能登入任何帳號
```

---

### E. 文件上傳安全

#### 在 backend_action.php 中
```php
// 1. 檢查副檔名
$file_ext = strtolower(pathinfo($original_name, PATHINFO_EXTENSION));
$allow = ['jpg', 'jpeg', 'png', 'webp'];

if (!in_array($file_ext, $allow)) {
    throw new Exception("不支援的圖片格式");
}

// 2. 檢查上傳過程是否出錯
if ($_FILES['product_images']['error'][$key] != 0) {
    throw new Exception("圖片上傳錯誤");
}

// 3. 重命名文件 (防止目錄遍歷攻擊)
$new_filename = $product_id . "_" . $key . "_" . time() . "." . $file_ext;

// 4. 移動到指定目錄
move_uploaded_file($tmp_name, $target_path);
```

**安全考量：**
| 措施 | 作用 |
|------|------|
| **副檔名白名單** | 只允許圖片格式 |
| **檢查 error 變數** | 確認上傳過程無誤 |
| **重新命名** | 避免使用原始名稱 |
| **儲存在 img/ 目錄** | 與源代碼分離 |

---

### F. XSS 防護

#### 在 database.php 中
```php
// 顯示數據時使用 htmlspecialchars()
echo '<td>' . htmlspecialchars((string)$data_row[$col_name]) . '</td>';

// 轉換示例：
// <script>alert('XSS')</script>
//   ↓
// &lt;script&gt;alert('XSS')&lt;/script&gt;
```

**作用：** 將 HTML 特殊字符轉為實體，瀏覽器不會執行 JavaScript

---

## 📊 用戶流程圖

### 一般消費者流程

```
┌─────────────────┐
│   訪問首頁      │
│   index.php     │
└────────┬────────┘
         │
    【已登入？】
      /         \
    是/          \否
    /              \
   │                └─ 點擊「登入」/「註冊」
   │                   │
   │              ┌────▼─────────────┐
   │              │  選擇操作         │
   │              ├─────────────────┤
   │              │ 1. 新用戶 → 註冊  │ → register.php
   │              │ 2. 既有 → 登入    │ → login.php
   │              └───────────────────┘
   │                   │
   │      【驗證成功】  │
   │                   │
   │              ┌────▼──────────────────┐
   │              │ $_SESSION['user_id']  │
   │              │ $_SESSION['user_name']│
   │              └────┬─────────────────┘
   │                   │
   │              ┌────▼────────────────┐
   │              │  首頁 (已登入狀態)   │
   │              │  • 瀏覽商品          │
   │              │  • 加入購物車        │
   │              │  • 結帳              │
   │              └────┬─────────────────┘
   │                   │
   │              【按登出】
   │                   │
   │              ┌────▼──────────────────┐
   │              │  logout.php           │
   │              │  • session_destroy()  │
   │              │  • 回首頁             │
   │              └───────────────────────┘
   │
   └─── 瀏覽商品、購物、搜尋等...
```

### 管理員流程

```
┌────────────────────┐
│  訪問後台登入頁    │
│  admin_login.php   │
└─────────┬──────────┘
          │
   【輸入帳號密碼】
          │
    ┌─────▼──────────────────┐
    │ 查詢 admin_users 表格   │
    └─────┬──────────────────┘
          │
     【帳號存在？】
        /      \
      是/        \否
      /           \
     │             └─ 錯誤: "找不到帳號"
     │
    【狀態=ACTIVE?】
      /      \
    是/        \否
    /           \
   │             └─ 錯誤: "帳號已停用"
   │
  【密碼驗證成功?】
    /      \
  是/        \否
  /           \
 │             └─ 錯誤: "密碼錯誤"
 │
【成功登入】
 │
┌▼────────────────────────┐
│ $_SESSION['admin_id']    │
│ $_SESSION['admin_username']│
│ $_SESSION['role_id']     │
└─┬────────────────────────┘
  │
┌─▼──────────────────────┐
│   後台首頁              │
│   backend.php           │
│                         │
│ • 新增商品表單         │
│ • 商品圖片上傳         │
│ • 分類選擇             │
│ • 庫存設置             │
└─┬──────────────────────┘
  │
【點擊「發布商品」】
  │
┌─▼──────────────────────┐
│  backend_action.php     │
│                         │
│ 1. 驗證表單數據         │
│ 2. 檢查圖片             │
│ 3. 開啟 Transaction     │
│ 4. 插入 products        │
│ 5. 插入 variants        │
│ 6. 上傳 & 插入圖片      │
│ 7. commit 或 rollback   │
└─┬──────────────────────┘
  │
【成功/失敗訊息】
  │
  └─ 返回 backend.php
```

### 數據庫查詢流程

```
管理員訪問 database.php
      │
 【非讀取操作】
      │
┌─────▼──────────────────────┐
│ SHOW TABLES                │
│ 獲取所有表格名稱            │
└─────┬──────────────────────┘
      │
 ┌────▼─────────────────────────────┐
 │ FOR EACH 表格                    │
 │  ├─ SHOW COLUMNS FROM {table}   │
 │  └─ SELECT * FROM {table}       │
 │     LIMIT 100                   │
 └────┬─────────────────────────────┘
      │
 ┌────▼──────────────────────────┐
 │ 產生 HTML 表格                 │
 │ • 表頭 (th)                    │
 │ • 數據行 (tr/td)               │
 │ • 防 XSS (htmlspecialchars)   │
 └──────────────────────────────┘
      │
      └─ 顯示在瀏覽器
```

---

## 📁 文件結構與依賴關係

```
suitcase_備份/
│
├─ 【前台】
│  ├─ index.php ────────────────► 首頁入口
│  │  ├─ 調用: database.php (連線)
│  │  └─ Session 檢查
│  │
│  ├─ login.php ────────────────► 用戶登入
│  │  ├─ 查詢: users 表格
│  │  ├─ 密碼驗證: password_verify()
│  │  └─ 設置: $_SESSION['user_id']
│  │
│  ├─ register.php ───────────────► 用戶註冊
│  │  ├─ 驗證: 密碼一致性
│  │  ├─ 加密: password_hash()
│  │  └─ 插入: users 表格
│  │
│  └─ logout.php ──────────────────► 用戶登出
│     ├─ 清除: $_SESSION
│     └─ 銷毀: session_destroy()
│
├─ 【後台】
│  ├─ admin_login.php ──────────► 管理員登入
│  │  ├─ 查詢: admin_users 表格
│  │  ├─ 檢查: 帳號狀態
│  │  ├─ 驗證: password_verify()
│  │  └─ 設置: $_SESSION['admin_id']
│  │
│  ├─ backend.php ────────────────► 後台商品管理
│  │  ├─ 驗證: admin Session
│  │  ├─ 查詢: categories 表格
│  │  └─ 顯示: 商品新增表單
│  │
│  ├─ backend_action.php ─────────► 商品新增處理
│  │  ├─ 驗證: 表單與圖片
│  │  ├─ 上傳: 圖片文件
│  │  ├─ 生成: SKU、Slug
│  │  ├─ 交易: BEGIN/COMMIT
│  │  ├─ 插入: products、variants、images
│  │  └─ 結果: 成功/失敗
│  │
│  └─ set_admin_account.php ──────► 管理員初始化
│     ├─ 生成: password_hash()
│     └─ 插入: admin_users 表格
│
├─ 【工具】
│  └─ database.php ──────────────────► 數據庫檢視工具
│     ├─ 查詢: 所有表格
│     ├─ 取得: 欄位信息
│     ├─ 防護: htmlspecialchars()
│     └─ 顯示: 表格數據 (LIMIT 100)
│
├─ 【資源文件】(預期)
│  ├─ img/
│  │  ├─ products/ ──────────────► 商品圖片存放位置
│  │  └─ 輪播1.png ─────────────► Hero 背景圖
│  │
│  ├─ css/ (預期，內嵌在 HTML 中)
│  └─ js/ (預期，可擴展)
│
└─ 【數據庫表格】(在 all_pass_db 中)
   ├─ users ─────────────────────► 一般用戶帳戶
   ├─ admin_users ───────────────► 管理員帳戶
   ├─ categories ─────────────────► 商品分類
   ├─ products ───────────────────► 商品基本信息
   ├─ product_variants ───────────► 商品變體 (SKU、價格、庫存)
   └─ product_images ─────────────► 商品圖片
```

---

## 🚀 快速開始指南

### 第一次設置

**1. 建立數據庫與表格**
```sql
CREATE DATABASE all_pass_db;
USE all_pass_db;

CREATE TABLE users (
    user_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password_hash VARCHAR(255),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE admin_users (
    admin_id INT PRIMARY KEY AUTO_INCREMENT,
    role_id INT,
    username VARCHAR(50) UNIQUE,
    password_hash VARCHAR(255),
    status ENUM('ACTIVE', 'INACTIVE', 'SUSPENDED') DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE categories (
    category_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100)
);

CREATE TABLE products (
    product_id INT PRIMARY KEY AUTO_INCREMENT,
    primary_category_id INT,
    name VARCHAR(200),
    slug VARCHAR(250) UNIQUE,
    is_featured TINYINT(1),
    status VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_variants (
    variant_id INT PRIMARY KEY AUTO_INCREMENT,
    product_id INT,
    sku_code VARCHAR(50) UNIQUE,
    price DECIMAL(10, 2),
    stock_available INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_images (
    image_id INT PRIMARY KEY AUTO_INCREMENT,
    product_id INT,
    image_url VARCHAR(255),
    is_main TINYINT(1),
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**2. 建立管理員帳號**
- 在瀏覽器訪問: `http://localhost/set_admin_account.php`
- 帳號: `admin` | 密碼: `adminOne01`
- 執行成功後刪除此文件

**3. 建立圖片上傳目錄**
```bash
mkdir img
mkdir img/products
chmod 777 img/products
```

**4. 訪問首頁**
- 前台: `http://localhost/index.php`
- 後台: `http://localhost/admin_login.php`

---

## 📝 常見問題 (FAQ)

### Q1: 如何修改管理員密碼？
```sql
-- 更新密碼前，先在 PHP 中 Hash：
-- $newpass = password_hash('newpassword', PASSWORD_DEFAULT);
UPDATE admin_users 
SET password_hash = '$2y$10$...' 
WHERE username = 'admin';
```

### Q2: 圖片上傳失敗
- ✅ 檢查 `img/products/` 目錄是否存在且有寫入權限
- ✅ 檢查上傳的文件是否 < 5MB
- ✅ 檢查檔案副檔名是否為 jpg/jpeg/png/webp

### Q3: 為什麼登入後還是被跳轉到登入頁？
- ✅ 檢查 PHP 版本 >= 7.0
- ✅ 確認 `session.save_path` 目錄存在且可寫
- ✅ 確認沒有 `session_destroy()` 被提前執行

### Q4: SQL 報錯「UNIQUE 約束違反」
- 這通常表示郵箱或 username 已存在
- 使用不同的郵箱註冊或檢查數據庫數據

### Q5: 如何備份數據庫？
```bash
mysqldump -u root all_pass_db > backup.sql
```

---

## 🔗 相關資源

| 資源 | 連結 |
|------|------|
| PHP 官方文檔 | https://www.php.net/ |
| MySQL 官方文檔 | https://dev.mysql.com/doc/ |
| password_hash() | https://www.php.net/manual/en/function.password-hash.php |
| mysqli 教程 | https://www.php.net/manual/en/book.mysqli.php |

---

**文檔版本：** 1.0  
**最後更新：** 2026 年 5 月 11 日  
**作者：** All Pass 開發團隊
